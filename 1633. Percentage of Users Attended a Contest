/* Write your PL/SQL query statement below */
SELECT CONTEST_ID, 
ROUND(COUNT(1) * 100 / (SELECT COUNT(1) FROM USERS),2) PERCENTAGE 
FROM REGISTER
GROUP BY CONTEST_ID
ORDER BY PERCENTAGE DESC, CONTEST_ID ASC
