/* Write your PL/SQL query statement below */
SELECT T1.USER_ID,
  ROUND(AVG(
    CASE
        WHEN T2.ACTION = 'confirmed' THEN 1.00 ELSE 0.00 
    END
),2)  CONFIRMATION_RATE
FROM SIGNUPS T1
LEFT JOIN CONFIRMATIONS T2
ON T1.USER_ID = T2.USER_ID
GROUP BY T1.USER_ID;
