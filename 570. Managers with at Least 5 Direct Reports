SELECT E.NAME FROM (
    SELECT MANAGERID , COUNT(MANAGERID)
    FROM EMPLOYEE 
    GROUP BY MANAGERID
    HAVING COUNT(MANAGERID) >= 5
) EM
JOIN EMPLOYEE E
ON E.ID = EM.MANAGERID


/*SELECT MANAGERID , COUNT(MANAGERID)
    FROM EMPLOYEE 
    GROUP BY MANAGERID
    HAVING COUNT(MANAGERID) >= 5*/
